---
phase: 01.2
plan: 03
type: execute
wave: 2
depends_on:
  - 01.2-01
requirements:
  - EMER-01
  - EMER-02
files_modified:
  - src/heretek_swarm/collective/metrics.py
  - src/heretek_swarm/collective/emergent_detection.py
  - src/heretek_swarm/collective/emergent_detection_utils.py
  - src/heretek_swarm/api/emergent_intelligence.py
autonomous: true
must_haves:
  truths:
    - "Collective intelligence metrics are collected periodically (every 5 minutes)"
    - "Weighted SIQ score applies validation rate weighting: raw_siq * validation_rate"
    - "Emergent patterns are detected and scored for novelty"
    - "Patterns are classified as PROVEN or UNPROVEN based on novelty and validation rate"
    - "API endpoint returns weighted SIQ with threshold_met indicator"
  artifacts:
    - path: "src/heretek_swarm/collective/metrics.py"
      provides: "Periodic SIQ collection and weighted scoring"
      contains: "start_metrics_collection,calculate_siq"
    - path: "src/heretek_swarm/collective/emergent_detection.py"
      provides: "Novelty scoring and emergence detection"
      contains: "NoveltyScorer,analyze_for_emergence"
    - path: "src/heretek_swarm/api/emergent_intelligence.py"
      provides: "Weighted SIQ API endpoint"
      contains: "/siq/weighted"
  key_links:
    - from: "src/heretek_swarm/collective/metrics.py"
      to: "CollectiveIntelligenceMetrics._siq_history"
      via: "Periodic collection in start_metrics_collection()"
      pattern: "start_metrics_collection"
    - from: "src/heretek_swarm/collective/emergent_detection.py"
      to: "EmergentPatternDetector._agent_snapshots"
      via: "analyze_for_emergence reads snapshots"
      pattern: "analyze_for_emergence"
    - from: "src/heretek_swarm/api/emergent_intelligence.py"
      to: "CollectiveIntelligenceMetrics"
      via: "GET /siq/weighted endpoint"
      pattern: "/siq/weighted"
---

# Plan: Emergent Intelligence Validation

**Requirements:** EMER-01 (Measurable Collective Intelligence), EMER-02 (Novel Solutions)
**Wave:** 2
**Priority:** P0/P1

---

## Context

EMER-01 and EMER-02 focus on validating emergent collective intelligence and novel solution detection. Research identified:

- **EMER-01:** `CollectiveIntelligenceMetrics.calculate_siq()` exists but needs periodic wiring. API endpoints at `/api/v1/emergent-intelligence/*` exist.
- **EMER-02:** `EmergentPatternDetector.analyze_for_emergence()` (emergent_detection.py:143-144) is a stub returning empty list. `NoveltyScorer` needs implementation.

**Key files:**
- `src/heretek_swarm/collective/metrics.py` (lines 507-591 — `calculate_siq`)
- `src/heretek_swarm/collective/emergent_detection.py` (lines 143-144 — stub `analyze_for_emergence`)
- `src/heretek_swarm/collective/emergent_detection_utils.py` (statistical significance utilities)
- `src/heretek_swarm/api/emergent_intelligence.py` (lines 88-123 — SIQ endpoint)

---

## Tasks

### Task 1: Wire SIQ Calculation to Periodic Interval (EMER-01.A)

<read_first>
- `src/heretek_swarm/collective/metrics.py` (lines 575-590 — history storage pattern)
- `src/heretek_swarm/actors/steward.py` (lines 395-403 — `_monitor_loop` for loop pattern)
</read_first>

<action>
Create metrics collection loop in `CollectiveIntelligenceMetrics`:
1. Add `async def start_metrics_collection(interval_seconds: float = 300)` method
2. Run `_metrics_loop()` that calls `calculate_siq()` every `interval_seconds`
3. Store results in `_siq_history` list (already exists, limit to 100 entries)
4. On exception, log and continue (don't stop collection)
</action>

<acceptance_criteria>
- `start_metrics_collection()` method exists in `CollectiveIntelligenceMetrics`
- Default interval: 300 seconds (5 minutes)
- `_metrics_loop()` calls `calculate_siq()` on each iteration
- History limited to 100 entries (drop oldest when full)
</acceptance_criteria>

---

### Task 2: Add Validation Event Tracking (EMER-01.B)

<read_first>
- `src/heretek_swarm/collective/metrics.py` (lines 570-580 — existing history pattern)
</read_first>

<action>
Add validation tracking to `CollectiveIntelligenceMetrics`:
1. Add `_validation_history: list[dict]` tracking validated SIQ measurements
2. Each entry: `{ "siq_value": float, "validated": bool, "timestamp": str, "validation_method": str }`
3. `validated` field set by Core Triad deliberation (or manual review for MVP)
4. Add `get_validation_rate() -> float` method: `count(validated=True) / total`
</action>

<acceptance_criteria>
- `_validation_history` list exists in `CollectiveIntelligenceMetrics`
- Each entry has fields: siq_value, validated, timestamp, validation_method
- `get_validation_rate()` method exists and returns float between 0.0 and 1.0
</acceptance_criteria>

---

### Task 3: Implement Weighted SIQ Scoring (EMER-01.C)

<read_first>
- `src/heretek_swarm/collective/metrics.py` (lines 507-591 — `calculate_siq` full method)
- `src/heretek_swarm/collective/metrics.py` (lines 559-573 — SwarmIntelligenceQuotient dataclass)
</read_first>

<action>
Modify `calculate_siq()` to apply validation weighting:
1. After calculating raw SIQ, check `_validation_history` for recent entries
2. If validation history exists, apply weighting: `weighted_siq = raw_siq * validation_rate`
3. If no history, use raw_siq unchanged
4. Store weighted_siq in result and history
</action>

<acceptance_criteria>
- `calculate_siq()` applies validation weighting when history exists
- Weighted score formula: `raw_siq * validation_rate`
- If no validation history, use raw_siq unchanged
- Result includes both raw and weighted scores
</acceptance_criteria>

---

### Task 4: Implement analyze_for_emergence() Stub (EMER-02.A)

<read_first>
- `src/heretek_swarm/collective/emergent_detection.py` (lines 143-144 — current stub)
- `src/heretek_swarm/collective/emergent_detection.py` (lines 50-90 — EmergentPattern dataclass)
- `src/heretek_swarm/collective/emergent_detection_utils.py` (statistical significance utilities)
</read_first>

<action>
Replace stub in `EmergentPatternDetector.analyze_for_emergence()`:
1. Collect agent snapshots from `self._agent_snapshots` (last 5 minutes)
2. Analyze for patterns: coordination, adaptation, knowledge sharing
3. Detect emergence using statistical significance testing
4. Return list of `EmergentPattern` objects meeting threshold
</action>

<acceptance_criteria>
- `analyze_for_emergence()` returns list of `EmergentPattern` objects (not empty list)
- Uses statistical significance testing from `emergent_detection_utils`
- Checks min_participating_agents threshold (config: analysis_window_seconds)
- Emergence level assigned based on coefficient value
</acceptance_criteria>

---

### Task 5: Implement Novelty Scoring (EMER-02.B)

<read_first>
- `src/heretek_swarm/collective/emergent_detection.py` (lines 30-50 — EmergentPatternClass enum)
- `src/heretek_swarm/collective/emergent_detection.py` (lines 50-90 — EmergentPattern dataclass)
</read_first>

<action>
Create `NoveltyScorer` class and integrate:
1. Create `class NoveltyScorer` in `emergent_detection.py`:
   - `__init__(known_patterns: list[EmergentPattern])` 
   - `score_pattern(pattern: EmergentPattern) -> float`: compare against known, return novelty 0.0-1.0
   - `update_known_patterns(pattern: EmergentPattern)`: add to known library
2. Novelty = 1.0 if pattern class not in known library, else 0.5 if significantly different, else 0.1
3. Integrate into `analyze_for_emergence()` output
</action>

<acceptance_criteria>
- `NoveltyScorer` class exists in `emergent_detection.py`
- Novelty scoring: new pattern class = 1.0, different from similar = 0.5, similar = 0.1
- `score_pattern()` returns float 0.0-1.0
- `update_known_patterns()` method adds patterns to library
</acceptance_criteria>

---

### Task 6: Add Proven/Unproven Classification (EMER-02.C)

<read_first>
- `src/heretek_swarm/collective/emergent_detection.py` (lines 50-90 — EmergentPattern dataclass)
- `src/heretek_swarm/collective/emergent_detection_types.py` (existing enums for pattern)
</read_first>

<action>
Modify emergent pattern classification:
1. Add `classification: EmergenceClassification = EmergenceClassification.UNPROVEN` to `EmergentPattern`
2. Enum: `class EmergenceClassification(Enum): PROVEN = "proven", UNPROVEN = "unproven"`
3. Unproven patterns: novelty score < 0.7 OR validation_rate < 0.6
4. Proven patterns: novelty score >= 0.7 AND validation_rate >= 0.6
5. Proven patterns eligible for impact score calculation
</action>

<acceptance_criteria>
- `EmergenceClassification` enum exists with PROVEN and UNPROVEN values
- `EmergentPattern.classification` field exists with default UNPROVEN
- Classification logic: PROVEN requires novelty >= 0.7 AND validation_rate >= 0.6
- Unproven patterns excluded from impact score (impact_score = 0.0)
</acceptance_criteria>

---

### Task 7: Add API Endpoint for Weighted SIQ (EMER-01)

<read_first>
- `src/heretek_swarm/api/emergent_intelligence.py` (lines 88-123 — existing SIQ endpoint)
</read_first>

<action>
Modify `src/heretek_swarm/api/emergent_intelligence.py`:
1. Add endpoint `GET /api/v1/emergent-intelligence/siq/weighted`
2. Returns `{ "siq": float, "validation_rate": float, "weighted_siq": float, "threshold_met": bool }`
3. threshold_met = weighted_siq >= 0.6
</action>

<acceptance_criteria>
- New endpoint `/api/v1/emergent-intelligence/siq/weighted` exists
- Response includes: siq, validation_rate, weighted_siq, threshold_met
- threshold_met = weighted_siq >= 0.6
</acceptance_criteria>

---

### Task 8: Add Tests for Emergent Intelligence (EMER-01, EMER-02)

<read_first>
- `tests/collective/test_session46_emergent_intelligence.py` (existing SIQ tests)
- `tests/collective/test_emergent_detection.py` (existing pattern tests)
</read_first>

<action>
Extend existing test files:
1. Add tests in `tests/collective/test_session46_emergent_intelligence.py`:
   - Test periodic SIQ collection
   - Test weighted SIQ calculation
   - Test validation rate tracking
2. Add tests in `tests/collective/test_emergent_detection.py`:
   - Test `analyze_for_emergence()` returns non-empty
   - Test NoveltyScorer scoring
   - Test Proven/Unproven classification
</action>

<acceptance_criteria>
- `pytest tests/collective/test_session46_emergent_intelligence.py` passes
- `pytest tests/collective/test_emergent_detection.py` passes
- Test coverage includes: SIQ weighted scoring, novelty detection, proven classification
- At least 3 new test functions per file
</acceptance_criteria>

---

## Verification

| Requirement | Verification Method | Threshold |
|-------------|---------------------|-----------|
| EMER-01: Collective intelligence | Weighted SIQ calculation | >= 0.6 |
| EMER-01: Validation tracking | Validation rate available | >= 0.6 weighted |
| EMER-02: Novel pattern detection | `analyze_for_emergence()` returns non-empty | >= 1 per window |
| EMER-02: Proven classification | Patterns classified correctly | PROVEN/UNPROVEN |

## Success Criteria

1. `CollectiveIntelligenceMetrics` has `start_metrics_collection()` running every 300s
2. Validation history tracked with `get_validation_rate()` method
3. Weighted SIQ = raw_siq * validation_rate, threshold >= 0.6
4. `analyze_for_emergence()` returns non-empty list with `EmergentPattern` objects
5. `NoveltyScorer` class with `score_pattern()` returning 0.0-1.0
6. `EmergenceClassification` enum with PROVEN/UNPROVEN, PROVEN requires novelty >= 0.7 AND validation >= 0.6
7. API endpoint `/api/v1/emergent-intelligence/siq/weighted` returns weighted SIQ
8. Tests pass for both EMER-01 and EMER-02 scenarios
