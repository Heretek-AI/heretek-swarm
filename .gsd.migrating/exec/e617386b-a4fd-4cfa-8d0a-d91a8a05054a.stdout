=== Docker HEALTHCHECK url ===
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-heretek_swarm}"]
      test: ["CMD", "redis-cli", "ping"]
      test: ["CMD-SHELL", "curl -sf http://localhost:6333/healthz || exit 1"]
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8222/ || nc -z localhost 4222 || exit 1"]
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]

=== Actual health endpoint in main.py ===
@app.get("/api/health")
@app.get("/api/health/live")
@app.get("/api/health/ready")

=== SPA path in code vs Docker ===
422:    """Mount React dashboard static files if dist directory exists."""
428:        "DASHBOARD_DIST_PATH",
429:        os.path.join(project_root, "dashboard", "frontend", "dist")
434:        logger.info("dashboard_spa_mounted", dist_path=dist_path)
436:        logger.warning("dashboard_dist_not_found", dist_path=dist_path)
1248:        "DASHBOARD_DIST_PATH",
1249:        os.path.join(project_root, "dashboard", "frontend", "dist")
1256:        logger.warning("dashboard_dist_not_found", dist_path=dist_path)
1280:        "DASHBOARD_DIST_PATH",
1281:        os.path.join(project_root, "dashboard", "frontend", "dist")

=== Dockerfile HEALTHCHECK ===
# Install curl for HEALTHCHECK
RUN apt-get update && apt-get install -y --no-install-recommends \
--
HEALTHCHECK --interval=30s --timeout=10s --retries=3 --start-period=15s \
    CMD curl -sf http://localhost:8000/health || exit 1
